# Healer → Patient Recommendation (backlog_issue intake)

> **Source:** Dyad-Healer (external observer). **Delivery:** the Frontier Operator files this on the
> patient repo — the Healer never opens issues directly. **Tier:** file at *intake* (this
> `backlog_issue` shape); the patient authors its own `node_contract` on acceptance.
> **Point-don't-fix:** Goal + Changes describe the *wound and direction*, never the implementation —
> the patient designs the suture. **Label on filing:** `external-recommendation` · `from-dyad-healer`
> · `<mode, e.g. prevention>`. Include a *suggested* path-mapping so it doesn't swell the unmapped backlog.

> **To author (low-inference):** fill the five fields below from the finding; everything else is fixed.

## Goal
{the requirement — what capability/gap, and *why it matters*}

## Changes
*(Direction only — the patient designs the implementation in its `node_contract`.)*
- {what should change, at the goal/behavior level — not code}

## Pre-Requisites
- {what must already be true / exist for this to be workable}

## Post-Requisites
*(Falsifiable success criteria — ideally a replay of the evidence.)*
- {observable test that proves the fix works}

## Depends On
- {patient primitives this grounds in: `WHY-`/`WHAT-`/`HOW-`}
- {Dyad-Healer ward evidence: `case-N` — the external attestation the patient can't self-generate}
