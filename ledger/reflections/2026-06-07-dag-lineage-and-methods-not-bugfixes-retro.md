# Retrospective (CSS) — DAG-lineage alignment + methods-not-bug-fixes

> **Audience:** Healer + Healer Operator · **Format:** Continue / Start / Stop (dyad-touchstone CSS).
> **Span:** since `cf41a56` (seizure-invariant retro) → this session. **Date:** 2026-06-07.

## Arc
Grounded `rack`/frontier vocabulary at its source (dyad-touchstone) → falsified cairn's + touchstone's
DAG mechanisms against our substrate → built the **survivor engine** `bin/dag.py` (frontier · todo · audit
stores; status structural; DONE excised; audit-lock = *Alignment Precedes Execution*; self-test-gated;
ASCII-tree render) → **disposed `rx_as_guards_A`** (the discard-invariant guard) on two converging rubs →
**clarified the craft-boundary**: dispense self-healing methods/guards, never bug-fix for the patient.

## CONTINUE
- **Agent details:** the **rub-chain** method worked — successive Operator falsifying rubs (rack-def →
  cairn lineage → todo → describe → over-intervention → bug-fix-vs-method) each built shared model
  *before* commitment. The 1+1=3: I generated grounding+build, the Operator validated by falsifying;
  **disposing `rx_as_guards_A` is a fit neither half walked in with.**
- **Operator narrative:** declined POV this retro.
- **Rub-of-the-CONTINUE (falsifying it):** "rubs worked great" is survivorship. They worked *because I
  grounded under them instead of defending*. A more fluently-wrong agent + the same chain could entrench
  a confident error faster. The real CONTINUE is **grounding-under-rub**, not the rub.

## START
- **Self-run uncertainty-is-law + methods-not-bug-fix at draft-time** on any patient-facing node — I built
  the *justification* for `rx_as_guards_A` and only dismantled it when pushed; the catch should've been mine.
- **`lean?`/`riff` = diverge then dialectically falsify to a SINGLE survived recommendation** (show the
  killed also-rans; don't leave alternatives open). → [[feedback_lean-riff-diverge-then-single-survivor]].

## STOP (mine)
- **Mapping canonical vocabulary from local usage** — I defined "rack" from backlog habit instead of
  grounding it at dyad-touchstone. Verify-before-asserting applies to *words*, at their source repo.
- **Advocacy-under-rub** — "describe the invariant *requiring* X" cued me to justify X; default is to *falsify* X.

## OPERATOR-STOP / rub-backs + dispositions
1. **rack ↔ dispose tension** → **Operator owns it:** wanted `todo`, had forgotten **rack was already
   deprecated**. Corroborated by cairn (rack_state.yml = dead orphan; `todo` is the survivor). →
   `dag.py`'s rack flag/commands are now mis-aligned; **parked as a todo** (`todo_1780873883`) to deprecate
   fresh, not fatigued. Memories corrected.
2. **`lean?` vs single-CTA** → **Operator:** `lean?`/`riff` are divergent but converge to a **single
   recommendation that survived dialectical falsification** — resolves the tension (still one recommendation,
   with the falsification shown).

## Forward
- **Parked (todo):** deprecate rack in `dag.py`; express dormancy via `todo` / plain READY-BLOCKED.
- **Stand down** — fatigue is real (self-flagged elevated error-rate), and the deliverable-frontier is
  honestly empty. Span harvested here.
